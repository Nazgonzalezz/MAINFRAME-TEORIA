//modelo de parcial
//1) Tengo el siguiente programa que número imprimira en pantalla?

 var contador=0,sumando=0;estado=false;
while(contador<=4)
    {
    if(estado)
        {
        sumando=sumando+1;
        estado=false;
        }
    else
        {
        sumando=sumando+2;
        estado=true;
        }
    contador++;
    }
console.log("Número :"+sumando);
//Indique el resultado de estas operaciones en Algebra de Boole:
//False && True = False
//True || False = True
//False || False = False
//False && False = false 

//Seleccione las opciones correctamente para que la consola imprima "El alumno está Aprobado":

var nota=8, presente=true;
if([presente] [==] [true])
  {
   if([nota] [>=] [7])
      console.log("El alumno está Aprobado");
   else
      console.log("El alumno va a recuperatorio");     
  }
else
  {
  console.log("El alumno si quiere aprobar debe presentarse al recuperatorio");
  }
  //Tenemos la función b=parseInt(a) y a="2":
 //Seleccione una o más de una:
a. parseInt convierte de texto a numero 
//b. el contenido de la variable a es un numero
//c. en b se almacenara el número 3
//d. el contenido de la variable a es texto
e. el contenido de la variable b sera un numero 
//f. parseInt convierte de numero a texto
//g. parseInt convierte de texto en texto

//Tengo la siguiente función que me indica el dia de la semana segun el numero que introduzco, ¿que imprimira en pantalla si elijo el valor 4?

var dia;
dia = parseInt(prompt("Indique numero de dia de la semana",));
switch(dia)
    {
    case 1 :
        {
        console.log("Domingo");
        break;
        }
    case 2 :
        {
        console.log("Lunes");
        }
    case 3 :
        {
        console.log("Martes");
        break;
        }
    case 4 :
        {
        console.log("Miércoles");
        }
    case 5 :
        {
        console.log("Jueves");
        break;
        }
    case 6 :
        {
        console.log("Viernes");
        }
    case 7 :
        {
        console.log("Sábado");
        break;
        }
}
//Escribimos un programa que calcula la intersección entre dos variables booleanas:
var a;
var b;
var resultado;
a = true ;
b = false ;
resultado = a && b ;
console.log("Resultado de a intersección b:"+(a || b));
//Que se imprimira en pantalla?
//Verdadero 